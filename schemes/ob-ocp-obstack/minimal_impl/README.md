# Minimal Impl

No source-derived minimal implementation is present for this scheme.

Reason: the verified OCP package exposes an `obstack` binary and version/provenance metadata, but no public OCP source tree was identified here. A minimal implementation derived from unknown source would be fabricated evidence, so this directory intentionally contains only this note.

Related but not substitutable: `../ob-open-obstack-ptrace/minimal_impl/` demonstrates the ptrace/libunwind-ptrace mechanics used by the public `oceanbase/obstack` source. It must not be counted as an OCP source-derived minimal implementation.
