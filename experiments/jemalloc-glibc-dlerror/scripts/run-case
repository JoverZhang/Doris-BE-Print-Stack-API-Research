#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
source "$script_dir/lib/run-case.sh"

if [[ $# -ne 5 ]]; then
    echo "usage: scripts/run-case <case> <ubuntu-version> <backend> <profiling> <expected>" >&2
    exit 2
fi

run_case_in_container "$@"
