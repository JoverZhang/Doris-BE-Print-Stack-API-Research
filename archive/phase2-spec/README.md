# Archive: Phase 2 Spec (Pre-Baseline-Pivot)

These three files are the earlier Phase 2 spec, archived on 2026-06-04.

- `phase2-design.md`: variant mechanics across all four candidates.
- `phase2-test-plan.md`: test cases under the prior contract.
- `phase2-acceptance.md`: pass/fail gates under the prior contract.

They were retired when the scope narrowed to the fp-walk baseline. The live
spec is now:

- `docs/architecture.md`: the variant-agnostic contract.
- `docs/phase2-test-plan.md`: fp-walk baseline test plan.
- `docs/phase2-acceptance.md`: fp-walk baseline acceptance checklist.

Refer back here when revisiting the other three variants
(`ck-phdr-unwind`, `ob-kill60`, `snapshot-remote-unwind`) in a later phase.
